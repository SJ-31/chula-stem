nextflow_process {

    name "Test Process MANTA"
    script "modules/manta.nf"
    process "MANTA"

    test("Should run without failures") {

        config "tests/nextflow.config"
        when {
            params {
                load("$baseDir/tests/wes_params.yaml")
            }
            process {
                """
                input[0] = [["out": "$params.out/manta",
                    "log": "$params.log/manta", "id": "sample2"],
                    "$params.out/bqsr/c_recal.bam",
                    "$params.out/bqsr/b_recal.bam"]
                input[1] = "$params.genome"
                input[2] = 4
                """
            }
        }

        then {
            assert process.success
            assert snapshot(process.out).match()
        }

    }

}
