nextflow_process {

    name "Test Process DELLY"
    script "modules/delly_sv.nf"
    process "DELLY_SV"

    test("Should run without failures") {

        when {
            params {
                load("$baseDir/tests/wes_params.yaml")
            }
            process {
                """
                input[0] = [["out": "$params.out/delly_sv", "RGSM_normal": "10",
                    "log": "$params.log/delly_sv", "id": "sample2"],
                    "$params.out/bqsr/b_recal.bam",
                    "$params.out/bqsr/c_recal.bam",
                    ["$params.out/bqsr/c_recal.bai", "$params.out/bqsr/b_recal.bai"]]
                input[1] = params.genome
                input[2] = ""
                input[3] = 4
                """
            }
        }

        then {
            assert process.success
        }

    }

}
