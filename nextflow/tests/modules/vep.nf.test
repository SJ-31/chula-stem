nextflow_process {

    name "Test Process VEP"
    script "modules/vep.nf"
    process "VEP"

    test("Should run without failures") {

        when {
            params {
                load("$baseDir/tests/wes_params.yaml")
            }
            process {
                """
                input[0] = [["out": "$params.out/vep",
                    "log": "$params.log/vep", "id": "sample2",
                    "RGSM_normal": "10"],
                    "$params.out2/patient_10/annotations/6-patient_10-Small_all.vcf.gz"]
                input[1] = "$params.genome_filtered"
                input[2] = 6
                """
            }
        }

        then {
            assert process.success
        }

    }

}
