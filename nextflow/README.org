:REFERENCES:
+ [1] https://support.illumina.com/content/dam/illumina-support/help/Illumina_DRAGEN_Bio_IT_Platform_v3_7_1000000141465/Content/SW/Informatics/Dragen/GPipelineAltMap_fDG.htm
+ [2] [[https://www.nature.com/articles/s41598-020-69772-8#Tab2][SomaticCombiner]]
+ [3] [[https://gatk.broadinstitute.org/hc/en-us/articles/360035531132--How-to-Call-somatic-mutations-using-GATK4-Mutect2][GATK Mutect2 Ho2]]
+ [4] [cite:@theicgc/tcgapan-canceranalysisofwholegenomesconsortiumPancancerAnalysisWhole2020]
:END:
* Workflows
# All workflows expect an input csv file with
Outputs are prefixed with "<module_number>-" representing their order in the pipeline (roughly)

Must be submitted to the job queue
** scRNA-seq
Standard scRNA-seq raw data processing worfklow
+ QC:
+ Mapping
** WES
Workflow for variant detection on WES data inspired by https://nf-co.re/sarek/3.4.4/ and GATK best practices
*** Input
+ A csv file with four fields
  * =patient= Patient id, which should be present twice, one for the tumor and another for the normal sample
  * =fastq_1= path to raw reads
  * =fastq_2=
  * =source=  [tumor|normal]
**** Reference data
+ Genome: hg38???
  * Needs to be indexed with =bwa-mem2 index= and =samtools faidx=
    * And =gatk CreateSequenceDictionary -R <ref>=
  * Contigs must also be named =chr1=, =chr2= and so on, for use with accucopy
+ Known variants. To use with bqsr, these must be indexed with =gatk IndexFeatureFile=
  * [[http://cancer.sanger.ac.uk/cosmic][COSMIC]] (see /data/project/sirasris_shared/)
  * [[https://gnomad.broadinstitute.org/downloads#exac][Broad Institute data]]
    * gnomAD v4.1.0 Exomes for all chromosomes
    * ExAC
      * Can't use this straight from the download, it is mapped to the GRCh37/hg19 reference
  * [[https://www.ncbi.nlm.nih.gov/projects/SNP/get_html.cgi?whichHtml=overview][dbSNP]]
***** Tool-specific requirements
- VEP: uses annotation sources to predict consequence types. Can be cached and downloaded with the =INSTALL.pl= script
  * Depends on what you put for the =species= flag
  * See [[https://grch37.ensembl.org/info/docs/tools/vep/script/vep_custom.html][here]] for how to supply custom annotation data
- snpEff: needs a reference-specific database for annotation (the second argument)
  * [[https://pcingola.github.io/SnpEff/snpeff/build_db/][Instructions for custom db]]
- GATK GetPileupSummaries: a vcf file of common germline variants, with only biallelic snps and a population allele frequence (AF) info tag
- bwa-mem2: alt index for for alt-aware mapping
- MISensorPro: a homopolymers and microsatellites file prepared from the target genome
- delly: bed exclusion file denoting chromosome regions to exclude from analysis and mappability file (for CNV version)
- gridss: bed exclusion file, recommended to be from the ENCODE project
- CNVkit: with exome data, needs BED file containing baited genomic regions of the target capture kit
- Picard: with exome data, needs BED file containing baited genomic regions of the target capture kit and the target intervals file
*** Steps
**** Preprocessing
+ QC: =fastp= with the following routines
  * Quality filtering
  * Length filtering
  * Base correction
  * polyG tail trimming: is performed if reads are detected to NextSeq/NovaSeq
+ Read mapping to reference: =bwa-mem2=
  * This is done in an alt-aware manner, due to the benefits for variant calling when doing so [1].
    * You can get the =.alt= index file for =bwa-mem2= at
+ Marking duplicates: GATK's =MarkDuplicatesSpark=
+ Base Quality Score Recalibration: GATK =BaseRecalibrator=  and =ApplyBQSR=
  * Reports changes to base scores with =AnalyzeCovariates=
  * Both cancer and normal samples undergo BQSR with the same =known_variants= sources, which will be sources of germline variants (listed fully above)
    * See [[https://gatk.broadinstitute.org/hc/en-us/articles/360035890831-Known-variants-Training-resources-Truth-sets][here]] for the details of known variants
**** Variant calling
+ Small variants
  * GATK Mutect2: somatic SNVs and indels
    * Output from Mutect2 is filtered according to best practices [3]
    * *Note:* Mutect2 raw vcf files do not use the QUAL field
  * [[https://github.com/Illumina/strelka][Strelka2]]: SNVs and indels, up to a predefined maximum size of (49 bases default)
    * Uses k
  * Notes
    * [1] observes that Mutect2 and Strelka2 are good at detecting SNVs in low VAF sites
  * [[https://github.com/wwylab/MuSE][MuSE2]]
  # * [[https://www.bioconductor.org/packages/release/bioc/html/PureCN.html][PureCN]]: copy number variation, tumor purity and ploidy
  # * [[https://github.com/etal/cnvkit][CNVkit]]: copy number variation
  #   * When given hybrid-selection data, utilizes off-target reads as well as on-target to detect CNVs
+ Structural variants
  * [[https://github.com/xjtu-omics/msisensor-pro][MISensorPro]]: microsatellite instability
  * [[https://github.com/Illumina/manta][Manta]]: Strutural variants (paired with strelka2)
  * [[https://github.com/dellytools/delly][delly]], uses split-read analysis
  * [[https://github.com/PapenfussLab/gridss][gridss]]: uses split-read analysis, and a
    * Can also report single breakends, "breakpoints in which only one side can be unambigiously determined"
  # * https://github.com/walaj/svaba Haven't done yet
+ Copy number variation
  * [[https://github.com/dellytools/delly][delly]]
    * delly has two separate nextflow processes (=DELLY_SV=  and =DELLY_CNV=)
    * Somatic CNV detection requires a read-depth profile of the tumor sample, generated from the tumor alignment file with the =DELLY_COV= process
  * [[https://github.com/mskcc/facets-suite][facets-suite]]
  *
****  Merging results
+ Concatenate results with =gatk MergeVcfs= and report source in =SOURCE= header
+ TODO: should probably have something more sophisticated than reporting everything, see notes
**** Callset quality control
+ Possible options based on [4]
  * Tumor in normal estimation
  * Filtering out somatic mutations that are known germline sites
  * Oxidative artifact filter
**** Metrics
+ Tumor purity and ploidy
  * With [[https://github.com/mskcc/facets-suite][facets-suite]]
    * See [cite:@aroraFACETSFractionAlleleSpecific2022] for how to interpret
+ QC metrics from =fastp=
+ Various metrics from =Picard=
  * =CountReads=
  * =CollectAlignmentSummaryMetrics=
  * TODO: fill this up
**** Variant annotation
  (both snpEff and VEP use the sequence ontology for consequence terms (see   [[http://asia.ensembl.org/info/genome/variation/prediction/predicted_data.html#consequences][here]] ))
- [[https://github.com/Ensembl/ensembl-vep?tab=readme-ov-file#vepusage][VEP]] (SNPs, indels, CNVs and SVs)
- [[https://pcingola.github.io/SnpEff/][snpEff]] (SNPs, indels, multiple-nucleotide-polymorphisms )
   * Make sure the the chromosome names of the snpEff db and your reference genome are identical, otherwise you will get missing chromosome errors
*** Output
+ Variants with high consequence, as predicted by VEP and snpEff (both using the same terms)

*** Motivation
+ Using multiple variant callers is common practice, notably used in the flagship analysis of the Pan-Cancer Analysis of Whole Genomes Consortium [4]
* Dependencies
https://bedops.readthedocs.io/en/latest/index.html
samtools, bcftools

# Local Variables:
# citar-prev-references: ("aroraFACETSFractionAlleleSpecific2022")
# End:
